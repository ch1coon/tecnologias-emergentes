# PostgreSQL local (porta 5432) — use se já tiver Postgres instalado na máquina
DATABASE_URL="postgresql://taskflow:taskflow@localhost:5432/taskflow?schema=public"

# PostgreSQL via Docker Compose (porta 5433 no host) — alternativa se a 5432 estiver ocupada
# DATABASE_URL="postgresql://taskflow:taskflow@localhost:5433/taskflow?schema=public"
PORT=3001
CORS_ORIGIN="http://localhost:5173"
